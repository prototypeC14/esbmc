# Quick Start: Python Pytest Test Generation

## 使用方法（超级简单！）

### 步骤 1: 准备你的Python代码

创建一个Python文件（比如 `my_code.py`）：

```python
def div1(cond: int, x: int) -> int:
    if not cond:
        return 42 // x  # ← 潜在的除零错误
    else:
        return x // 10

# 测试代码
cond = 0
x = 0
result = div1(cond, x)
```

### 步骤 2: 运行ESBMC生成pytest测试

```bash
esbmc my_code.py --generate-testcase
```

就这样！ESBMC会自动：
1. 🔍 分析你的代码
2. 🐛 发现bug（除零错误）
3. ✍️ **自动生成** pytest测试文件

### 步骤 3: 查看生成的测试

ESBMC会创建 `test_counterexample.py`：

```python
"""
Auto-generated pytest test file from ESBMC counterexample.

This test reproduces a bug found by ESBMC symbolic execution.
Generated by ESBMC 7.7

Violation: division by zero
Property: x != 0
"""
import pytest
from my_code import div1


def test_div1_counterexample():
    """
    Test case generated from ESBMC counterexample.

    This test should trigger the bug found by ESBMC.
    Run with: pytest test_counterexample.py
    """
    # Input values from counterexample
    cond = 0
    x = 0

    # This call should reproduce the bug
    with pytest.raises(ZeroDivisionError):
        result = div1(cond, x)


def test_div1_manual():
    """
    Manual test - customize this to verify the fix.
    """
    # TODO: Add test cases for correct behavior after fixing the bug
    pass
```

### 步骤 4: 运行生成的测试

```bash
pytest test_counterexample.py -v
```

输出：
```
test_counterexample.py::test_div1_counterexample PASSED ✓
test_counterexample.py::test_div1_manual SKIPPED

========================= 1 passed, 1 skipped =========================
```

## 完整示例

```bash
# 1. 创建Python文件
cat > example.py << 'EOF'
def process(x: int) -> int:
    return 100 / x  # Bug: division by zero when x=0

x = 0
result = process(x)
EOF

# 2. 生成测试
esbmc example.py --generate-testcase

# 3. 运行测试
pytest test_counterexample.py

# 输出:
# ✓ test_process_counterexample PASSED (correctly caught ZeroDivisionError)
```

## 命令行选项

### 基础用法
```bash
esbmc example.py --generate-testcase
```

### 多个counterexample（生成多个测试文件）
```bash
esbmc example.py --multi-property --generate-testcase

# 输出:
# test_counterexample_1.py
# test_counterexample_2.py
# test_counterexample_3.py
```

### 结合覆盖率分析
```bash
esbmc example.py --branch-coverage --generate-testcase

# ESBMC会:
# 1. 分析分支覆盖率
# 2. 为未覆盖的分支生成测试用例
```

## 支持的Python bug类型

ESBMC会自动识别异常类型：

| Bug类型 | ESBMC检测 | pytest异常 |
|---------|-----------|-----------|
| 除零错误 | `division by zero` | `ZeroDivisionError` |
| 数组越界 | `array bounds` | `IndexError` |
| 断言失败 | `assertion` | `AssertionError` |
| 其他 | - | `Exception` |

## FAQ

### Q: 我需要安装pytest吗？
A: 运行生成的测试需要，但ESBMC生成测试不需要。

```bash
pip install pytest  # 只在运行测试时需要
```

### Q: 生成的测试文件在哪里？
A: 当前目录下，文件名为：
- 单个: `test_counterexample.py`
- 多个: `test_counterexample_1.py`, `test_counterexample_2.py`, ...

### Q: C语言还能用吗？
A: 完全可以！`--generate-testcase` 会自动检测：
- `.py` 文件 → 生成 pytest 测试
- `.c` 文件 → 生成 XML 测试（Test-Comp格式）

### Q: 如何自定义测试？
A: 编辑生成的 `test_counterexample.py`：
- `test_*_counterexample()` - ESBMC生成的bug复现测试
- `test_*_manual()` - 手动编辑，添加修复后的测试

## 示例文件

仓库包含两个示例：

```bash
# 除零错误示例
esbmc example_div_bug.py --generate-testcase

# 断言失败示例
esbmc example_assert_bug.py --generate-testcase
```

## 完整工作流程

```
你的Python代码
    ↓
esbmc code.py --generate-testcase
    ↓
分析 + 发现bug
    ↓
生成 test_counterexample.py
    ↓
pytest test_counterexample.py
    ↓
✓ 测试通过（bug已复现）
    ↓
修复代码
    ↓
pytest test_counterexample.py
    ↓
✗ 测试失败（现在不抛出异常了 = bug已修复！）
```

## 更多信息

详细文档请参考：[PYTEST_GENERATION.md](PYTEST_GENERATION.md)
