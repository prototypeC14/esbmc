# SV-COMP Bool 类型标准

## 修改说明

按照 **SV-COMP Coverage Competition** 标准，`__VERIFIER_nondet_bool()` 函数应该使用 C99 的 `_Bool` 类型，而不是 `int` 类型。

## SV-COMP 标准格式

### 正确的声明 ✅

```c
extern _Bool __VERIFIER_nondet_bool(void);
```

### 错误的声明 ❌

```c
extern int __VERIFIER_nondet_bool(void);  // 不符合 SV-COMP 标准
```

## 生成的代码格式

### 修改前 (错误)

```c
int __VERIFIER_nondet_bool(void) {
  static int i = 0;
  static const int v[] = { 0, 1 };
  return v[i++];
}
```

### 修改后 (正确) ✅

```c
_Bool __VERIFIER_nondet_bool(void) {
  static int i = 0;
  static const _Bool v[] = { 0, 1 };
  return v[i++];
}
```

## 代码修改

### 修改文件：src/goto-symex/ctest.cpp

```cpp
// 修改前
else if (is_bool_type(type))
{
  return "int";  // C uses int for bool
}

// 修改后
else if (is_bool_type(type))
{
  return "_Bool";  // SV-COMP standard: use C99 _Bool type
}
```

## C99 _Bool 类型说明

### 什么是 _Bool？

`_Bool` 是 C99 标准引入的原生布尔类型：
- 只能存储 0 或 1
- 任何非零值赋给 `_Bool` 都会被转换为 1
- 占用 1 字节（通常）

### stdbool.h

C99 还提供了 `<stdbool.h>` 头文件，定义了：
```c
#define bool _Bool
#define true 1
#define false 0
```

但 SV-COMP 标准要求直接使用 `_Bool` 类型名。

## 测试用例

### test_bool_svcomp.c

```c
#include <stdio.h>

// 按照 SV-COMP 标准声明
extern _Bool __VERIFIER_nondet_bool(void);

int main(void) {
  _Bool flag1 = __VERIFIER_nondet_bool();
  _Bool flag2 = __VERIFIER_nondet_bool();

  if (flag1 && flag2) {
    return 1;
  } else if (flag1 || flag2) {
    return 2;
  } else {
    return 0;
  }
}
```

### 生成测试用例

```bash
./esbmc test_bool_svcomp.c --branch-coverage --generate-ctest-testcase
cat test_case_1.c
```

**预期输出：**
```c
// Auto-generated by ESBMC
// Test case 1 of 4

_Bool __VERIFIER_nondet_bool(void) {
  static int i = 0;
  static const _Bool v[] = { 1, 1 };  // flag1=1, flag2=1
  return v[i++];
}
```

## 兼容性

### 编译器支持

| 编译器 | C99 _Bool 支持 |
|--------|---------------|
| GCC 3.0+ | ✅ 完全支持 |
| Clang | ✅ 完全支持 |
| MSVC 2013+ | ✅ 支持（需 /std:c11） |

### 编译选项

确保使用 C99 或更新标准：
```bash
gcc -std=c99 test_bool_svcomp.c test_case_1.c -o test
```

或使用 C11：
```bash
gcc -std=c11 test_bool_svcomp.c test_case_1.c -o test
```

## SV-COMP 参考

SV-COMP (Competition on Software Verification) 是国际软件验证竞赛，定义了标准的 `__VERIFIER_*` API。

### 官方文档

- SV-COMP 规则: https://sv-comp.sosy-lab.org/
- VERIFIER API: https://github.com/sosy-lab/sv-benchmarks/blob/main/c/VERIFIER.md

### 相关函数声明（SV-COMP标准）

```c
extern void __VERIFIER_assume(_Bool);
extern void __VERIFIER_assert(_Bool);
extern _Bool __VERIFIER_nondet_bool(void);
extern int __VERIFIER_nondet_int(void);
extern unsigned int __VERIFIER_nondet_uint(void);
// ... 其他类型
```

注意：`__VERIFIER_assume()` 和 `__VERIFIER_assert()` 的参数也是 `_Bool` 类型！

## 向后兼容性

由于 `_Bool` 和 `int` 在大多数情况下可以互换使用，这个修改应该不会破坏现有代码。但为了严格符合 SV-COMP 标准，建议：

1. **测试代码使用 `_Bool` 声明**
2. **编译时使用 C99 或更新标准**
3. **检查生成的测试用例格式**

## 验证修改

### 检查生成的代码

```bash
# 生成测试用例
./esbmc test_bool_svcomp.c --generate-ctest-testcase

# 验证类型正确
grep "_Bool __VERIFIER_nondet_bool" test_case.c

# 应该看到：
# _Bool __VERIFIER_nondet_bool(void) {
```

### 编译测试

```bash
# 应该能成功编译
gcc -std=c99 test_bool_svcomp.c test_case.c -o test
./test
echo $?  # 查看返回值
```

## 总结

这个修改确保 ESBMC 生成的 CTest 测试用例完全符合 SV-COMP Coverage Competition 的标准，使用 C99 `_Bool` 类型而不是 `int` 类型来表示布尔值。

---

**修改日期:** 2025-12-28
**相关 Commit:** 683c7a943
**符合标准:** SV-COMP 2024/2025
