# CTest 测试生成使用指南

## 快速开始

### 1. 编写包含非确定性输入的 C 程序

```c
// example.c
#include <assert.h>

extern int __VERIFIER_nondet_int();

int divide(int a, int b) {
    assert(b != 0);
    return a / b;
}

int main() {
    int x = __VERIFIER_nondet_int();
    int y = __VERIFIER_nondet_int();

    int result = divide(x, y);
    return 0;
}
```

### 2. 使用 ESBMC 生成测试

```bash
# 单个测试用例
esbmc example.c --generate-ctest-testcase

# 多个测试用例（覆盖率模式）
esbmc example.c --branch-coverage --generate-ctest-testcase
```

### 3. 编译并运行测试

```bash
mkdir build && cd build
cmake ..
make
ctest
```

### 4. 生成覆盖率报告

```bash
# 启用覆盖率重新编译
cmake -DENABLE_COVERAGE=ON ..
make
ctest

# 生成 HTML 报告
lcov --capture --directory . -o coverage.info
genhtml coverage.info -o coverage_html
```

## 支持的非确定性函数

- `__VERIFIER_nondet_int()` - 整数
- `__VERIFIER_nondet_uint()` - 无符号整数
- `__VERIFIER_nondet_char()` - 字符
- `__VERIFIER_nondet_uchar()` - 无符号字符
- `__VERIFIER_nondet_short()` - 短整型
- `__VERIFIER_nondet_ushort()` - 无符号短整型
- `__VERIFIER_nondet_long()` - 长整型
- `__VERIFIER_nondet_ulong()` - 无符号长整型
- `__VERIFIER_nondet_float()` - 浮点数
- `__VERIFIER_nondet_double()` - 双精度浮点数
- `__VERIFIER_nondet_bool()` - 布尔值

## 生成的测试文件结构

### 单个测试模式
```
.
├── test_case.c          # 独立的测试程序
└── CMakeLists.txt       # CTest 配置
```

### 覆盖率模式
```
.
├── test_case_1.c        # 测试用例 1
├── test_case_2.c        # 测试用例 2
├── test_case_N.c        # 测试用例 N
└── CMakeLists.txt       # 所有测试的配置
```

## 生成的测试文件示例

**test_case.c:**
```c
// Auto-generated by ESBMC
// Source: example.c

#include <stdio.h>
#include <assert.h>

int main() {
    int x = 10;  // 从反例中提取的具体值
    int y = 0;   // 触发断言失败的值

    // 调用被测函数
    divide(x, y);

    return 0;
}
```

**CMakeLists.txt:**
```cmake
# Auto-generated by ESBMC
cmake_minimum_required(VERSION 3.10)
project(ESBMCGeneratedTests)

# 覆盖率选项
option(ENABLE_COVERAGE "Enable coverage reporting" OFF)
if(ENABLE_COVERAGE)
    set(CMAKE_C_FLAGS "${CMAKE_C_FLAGS} --coverage")
    set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} --coverage")
endif()

enable_testing()

# 测试用例
add_executable(test_case_1 test_case_1.c)
add_test(NAME test_case_1 COMMAND test_case_1)
set_tests_properties(test_case_1 PROPERTIES WILL_FAIL TRUE)
```

## 关键特性

### 1. WILL_FAIL 属性
测试被标记为 `WILL_FAIL TRUE`，意味着：
- 测试**应该**失败（触发断言）
- CTest 会将失败视为"通过"
- 用于验证错误输入确实能重现问题

### 2. 覆盖率集成
生成的 CMakeLists.txt 包含：
- `--coverage` 编译标志
- 支持 gcov/lcov 覆盖率工具
- 可以验证测试集的实际覆盖率

### 3. 独立测试
每个测试用例是独立的可执行文件：
- 完全隔离，不会相互影响
- 可以单独调试
- 真实的执行环境

## 常见用例

### 1. 安全性测试
```c
extern int __VERIFIER_nondet_int();

void buffer_copy(char *dst, char *src, int len) {
    assert(len > 0 && len < 1024);  // 边界检查
    memcpy(dst, src, len);
}
```

### 2. 状态机验证
```c
extern int __VERIFIER_nondet_int();

typedef enum { IDLE, RUNNING, STOPPED } state_t;

void state_machine(state_t *state, int event) {
    assert(*state != RUNNING || event != STOP_EVENT);
    // 状态转换逻辑
}
```

### 3. 数值计算验证
```c
extern float __VERIFIER_nondet_float();

float safe_sqrt(float x) {
    assert(x >= 0);  // 负数无法开方
    return sqrtf(x);
}
```

## 与 pytest 的对比

| 方面 | pytest (Python) | ctest (C) |
|------|----------------|-----------|
| 输入方式 | `nondet_int()` | `__VERIFIER_nondet_int()` |
| 测试格式 | 参数化函数 | 独立可执行文件 |
| 编译 | 不需要 | 需要 CMake + 编译器 |
| 运行 | `pytest` | `ctest` |
| 覆盖率 | `pytest-cov` | `gcov/lcov` |

## 调试技巧

### 1. 单独运行测试
```bash
./build/test_case_1
```

### 2. 使用 GDB 调试
```bash
gdb ./build/test_case_1
(gdb) run
(gdb) backtrace
```

### 3. 查看具体覆盖率
```bash
gcov test_case_1.gcda
cat test_case_1.c.gcov
```

## 注意事项

1. **需要原始源代码**：生成的测试需要链接原始函数
2. **断言失败是预期的**：`WILL_FAIL TRUE` 标记
3. **覆盖率验证**：确认 ESBMC 生成的测试确实覆盖了目标路径
4. **编译依赖**：需要 CMake 3.10+ 和 C 编译器

## 完整工作流程示例

```bash
# 1. 生成测试
esbmc example.c --branch-coverage --generate-ctest-testcase

# 2. 编译测试
mkdir build && cd build
cmake -DENABLE_COVERAGE=ON ..
make

# 3. 运行测试
ctest

# 4. 生成覆盖率报告
lcov --capture --directory . --output-file coverage.info
genhtml coverage.info --output-directory coverage_html

# 5. 查看报告
firefox coverage_html/index.html
```

## 高级用法

### 并行测试
```bash
ctest -j4  # 使用 4 个并行作业
```

### 详细输出
```bash
ctest -V  # 显示详细输出
```

### 过滤测试
```bash
ctest -R test_case_1  # 只运行匹配的测试
```

### 重新运行失败的测试
```bash
ctest --rerun-failed
```
