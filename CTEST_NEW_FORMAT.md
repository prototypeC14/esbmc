# CTest Generation - New Format

## 概述

CTest生成器现在采用新的策略：生成 `__VERIFIER_nondet_*` 函数的具体实现，而不是独立的main程序。

## 设计原理

### 旧格式（已废弃）
```c
// test_case_1.c - 独立的测试程序
int main() {
    int value_0 = 1;
    int value_1 = 0;
    return 0;
}
```

### 新格式（当前）
```c
// test_case_1.c - 提供__VERIFIER_nondet_*实现
int __VERIFIER_nondet_int(void) {
  static int i = 0;
  static const int v[] = { 1, 0 };  // a=1, b=0
  return v[i++];
}
```

## 优势

1. **真实测试原始代码**：保持原始代码不变，包括所有函数调用和逻辑
2. **准确的覆盖率**：覆盖率报告直接反映原始代码的执行情况
3. **SV-COMP兼容**：符合SV-COMP的标准格式
4. **支持多次调用**：正确处理同一个nondet函数的多次调用

## 使用示例

### 1. 准备源代码

```c
// example.c
#include <stdio.h>

extern int __VERIFIER_nondet_int(void);

static int compute(int a, int b) {
  if (a > 0) {
    if (b == 0) {
      return 10;
    } else {
      return 11;
    }
  } else {
    if (b < 0) {
      return 20;
    } else {
      return 21;
    }
  }
}

int main(void) {
  int a = __VERIFIER_nondet_int();
  int b = __VERIFIER_nondet_int();
  int r = compute(a, b);
  printf("%d\n", r);
  return 0;
}
```

### 2. 生成测试用例

```bash
# 使用分支覆盖模式生成多个测试用例
esbmc example.c --branch-coverage --generate-ctest-testcase
```

### 3. 生成的文件结构

```
test_case_1.c    # __VERIFIER_nondet_int实现 (a=1, b=0)
test_case_2.c    # __VERIFIER_nondet_int实现 (a=1, b=1)
test_case_3.c    # __VERIFIER_nondet_int实现 (a=-1, b=-1)
test_case_4.c    # __VERIFIER_nondet_int实现 (a=-1, b=0)
CMakeLists.txt   # 编译配置
```

### 4. test_case_1.c 示例

```c
// Auto-generated by ESBMC
// Test case 1 of 4
// This file provides concrete implementations of __VERIFIER_nondet_* functions

int __VERIFIER_nondet_int(void) {
  static int i = 0;
  static const int v[] = { 1, 0 };
  return v[i++];
}
```

### 5. CMakeLists.txt 示例

```cmake
# Auto-generated by ESBMC
cmake_minimum_required(VERSION 3.10)
project(ESBMCGeneratedTests C)

enable_testing()

option(ENABLE_COVERAGE "Enable coverage reporting" OFF)
if(ENABLE_COVERAGE AND CMAKE_C_COMPILER_ID MATCHES "GNU|Clang")
  add_compile_options(-O0 -g --coverage)
  add_link_options(--coverage)
endif()

# Each test case is compiled with the original source + test case implementation
add_executable(test_case_1 example.c test_case_1.c)
add_test(NAME test_case_1 COMMAND test_case_1)

add_executable(test_case_2 example.c test_case_2.c)
add_test(NAME test_case_2 COMMAND test_case_2)

add_executable(test_case_3 example.c test_case_3.c)
add_test(NAME test_case_3 COMMAND test_case_3)

add_executable(test_case_4 example.c test_case_4.c)
add_test(NAME test_case_4 COMMAND test_case_4)
```

### 6. 编译和运行

```bash
mkdir -p build
cd build
cmake -DENABLE_COVERAGE=ON ..
cmake --build .
ctest --output-on-failure
```

### 7. 生成覆盖率报告

```bash
# 使用gcovr（推荐）
gcovr -r . --branches --object-directory build --gcov-executable "llvm-cov gcov"

# 或使用lcov
lcov --capture --directory . --output-file coverage.info
genhtml coverage.info --output-directory coverage_html
```

## 支持的类型

生成器自动识别并支持以下 `__VERIFIER_nondet_*` 类型：

| VERIFIER类型 | C类型 | 示例 |
|------------|------|------|
| int | int | -10, 0, 127 |
| uint | unsigned int | 0, 255, 1000 |
| char | char | -128, 0, 127 |
| uchar | unsigned char | 0, 255 |
| short | short | -32768, 0, 32767 |
| ushort | unsigned short | 0, 65535 |
| long | long long | -2147483648, 0 |
| ulong | unsigned long long | 0, 4294967295 |
| float | float | 0.0, 3.14, -1.5 |
| double | double | 0.0, 2.718281828 |
| bool | int | 0, 1 |

## 多类型示例

如果代码使用多种nondet类型：

```c
int x = __VERIFIER_nondet_int();
float y = __VERIFIER_nondet_float();
```

生成的 test_case.c 会包含：

```c
int __VERIFIER_nondet_int(void) {
  static int i = 0;
  static const int v[] = { 42 };
  return v[i++];
}

float __VERIFIER_nondet_float(void) {
  static int i = 0;
  static const float v[] = { 3.14 };
  return v[i++];
}
```

## 覆盖率分析

每个测试用例独立运行：
- 生成自己的 `.gcda` 文件
- gcovr/lcov 合并所有测试的覆盖率数据
- 最终报告显示总体代码覆盖情况

```bash
# 查看分支覆盖率
gcovr -r . --branches

# 生成HTML报告
gcovr -r . --branches --html --html-details -o coverage.html
```

## 与pytest对比

| 特性 | CTest (C) | pytest (Python) |
|------|-----------|-----------------|
| 测试策略 | 提供nondet函数实现 | 参数化测试函数 |
| 编译方式 | 原始代码 + 测试用例 | 导入模块 |
| 覆盖率工具 | gcov/gcovr | pytest-cov |
| 测试运行 | CTest | pytest |
| 适用场景 | C程序，main函数测试 | Python函数测试 |

## 命令行选项

```bash
# 单个测试用例（错误发现模式）
esbmc program.c --generate-ctest-testcase

# 多个测试用例（覆盖率模式）
esbmc program.c --branch-coverage --generate-ctest-testcase

# 断言覆盖模式
esbmc program.c --assertion-coverage --generate-ctest-testcase
```

## 调试技巧

1. **查看生成的测试用例**：
   ```bash
   cat test_case_1.c
   ```

2. **单独编译测试**：
   ```bash
   gcc -o test example.c test_case_1.c
   ./test
   ```

3. **查看测试输出**：
   ```bash
   ctest --verbose
   ```

4. **检查覆盖率数据**：
   ```bash
   gcovr --print-summary
   ```
